# Servidor
PORT=3000
NODE_ENV=development

# Base de datos PostgreSQL (se configurará al incorporar Sequelize)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=codart
DB_USER=postgres
DB_PASSWORD=change_me

# Autenticación (se configurará al crear el módulo Auth)
JWT_SECRET=replace_with_a_long_random_secret
JWT_EXPIRES_IN=1d
